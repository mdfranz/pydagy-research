# Python

Use `uv` and Python 3.14


# Git rules

- You may commit (use my user.name not an Agent) but `NOT` push!
- Keep commits verbose

# LLM Provider and Model Selection

Use `google:gemini-3.7-flash` as the default model
